services:
    db:
        image: postgres:17
        env_file:
          - .env
        ports:
          - "${POSTGRES_PORT:-5432}:5432"
        volumes:
          - data:/var/lib/postgresql/data

volumes:
    data:
